<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <script src="./lib/vue.js">
    </script>
</head>
<body>
<div id="app">
    <my-com1></my-com1>
    <mycom2></mycom2>
    <mycom3></mycom3>
</div>
<template id="tmp1">
    <div>
        <h3>这是第三种方式</h3>
    </div>
</template>
<script>
    // 注意  组件模版里面只有唯一的根元素
    // 1 方式1
    var com1=Vue.extend({
      template:'<h3>这是extend创建出来的组件</h3>'
    })
    // 驼峰命名，在引用时候大写改为小写，同时单词之间使用-连接
    Vue.component('myCom1',com1)
    // 方式2
    Vue.component('mycom2',{
      template:'<h3>这是com2创建出来的组件</h3>'
    })
    // 方式3
    Vue.component('mycom3',{
      template:'#tmp1'
    })

    var vm = new Vue({
      el:'#app',
      data(){
        return {

        }
      },
      methods:{},
      components:{   //定义私有组件
        login:{
          template:"<h1>这是私有组件</h1>"
        }
      }
    })
</script>
</body>
</html>